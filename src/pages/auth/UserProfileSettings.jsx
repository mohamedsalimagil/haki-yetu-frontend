import React from 'react';
import UserProfileSettings from '../../components/domain/auth/UserProfileSettings';

const ProfileSettings = () => {
  return <UserProfileSettings />;
};

export default ProfileSettings;
